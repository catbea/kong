import * as types from '@/store/mutation-types'

const state = {
  cityMap:{}
}

const getters = {
  cityMap: state => state.cityMap
}

const actions = {

}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}